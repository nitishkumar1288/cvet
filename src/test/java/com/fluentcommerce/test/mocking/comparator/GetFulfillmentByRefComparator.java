package com.fluentcommerce.test.mocking.comparator;

import com.fluentcommerce.graphql.query.fulfilment.GetFulfillmentByRefQuery;

import java.util.Comparator;

public class GetFulfillmentByRefComparator implements Comparator<GetFulfillmentByRefQuery> {
    @Override
    public int compare(GetFulfillmentByRefQuery o1, GetFulfillmentByRefQuery o2) {
        return 0;
    }
}
